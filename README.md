# 🐶 SniffHub (강아지 유치원 관리 시스템)

**객체지향언어2 팀 프로젝트**
**Java Swing 기반의 데스크톱 애플리케이션**

### 🧑‍💻 개발 팀원
| **[문세연]** | **View & Controller** (UI 구현, 신규 등록 및 검색 로직 기능 구현) |<br/>
| **[양재웅]** | **Model & DB** (DB 스키마 설계, DAO 구현, SQL 작성) |<br/>
| **[송지원]** | **Model & Controller** (MVC 구조 설계, 출석 관리 로직 기능 구현) |

---

## 📖 1. 프로젝트 소개
**SniffHub**는 관리자가 강아지들의 등/하원 및 상태를 체계적으로 관리하기 위해 개발되었습니다.
**MVC (Model-View-Controller) 아키텍처**를 적용하여 유지보수성과 확장성을 높였으며, **SQLite 데이터베이스**를 연동하여 데이터를 영구적으로 저장 및 관리합니다.

### 💡 핵심 기능
* **신규 등록 :** 보호자와 강아지 정보를 입력하면, 강아지 나이에 따라 **자동으로 반(사회반/놀이반/교육반)이 배정**됩니다.
* **통합 검색 :** 보호자 이름으로 강아지를 검색하고, 정보를 **수정하거나 삭제**할 수 있습니다.
* **출석 관리 :** 반별로 강아지 리스트를 조회하고,  **출석/식사 여부(체크박스)** 및 **훈련 참여도(드롭다운)**를 체크하여 DB에 일괄 저장합니다.

---

## 🛠 2. 개발 환경
별도의 설치 없이 바로 실행 가능하도록 내장형 DB(SQLite)를 사용했습니다.

* **Language:** Java (JDK 17 권장)
* **GUI Framework:** Java Swing
* **Database:** SQLite (프로젝트 내 `sniffhub.db` 포함)
* **IDE:** IntelliJ IDEA
* **Architecture:** MVC Design Pattern

---

## 🚀 3. 실행 방법
**별도의 라이브러리 다운로드가 필요 없습니다.**
프로젝트 폴더 내에 필요한 드라이버와 DB 파일이 모두 포함되어 있습니다.

### 1단계: 프로젝트 열기
압축을 푼 후, IntelliJ IDEA에서 **`project_sniffhub`** 폴더를 엽니다.

### 2단계: 라이브러리 확인 (자동 설정되지 않았을 경우)
프로젝트 내 **`lib`** 폴더에 있는 `sqlite-jdbc-x.x.x.jar` 파일이 라이브러리로 잡혀있는지 확인해주세요.
* *설정 방법: `File` -> `Project Structure` -> `Modules` -> `Dependencies` -> `+` -> `JARs or Directories` -> `lib` 폴더 선택*

### 3단계: 프로젝트 실행하기
아래 경로의 파일을 찾아 실행해주세요.
> **`src/main/java/com/sniffhub/view/Main.java`**
관리자 ID 0000을 입력하시면 로그인이 됩니다.
---

## 📂 4. 프로젝트 폴더 구조
MVC 패턴에 따라 패키지가 명확하게 분리되어 있습니다.

```text
project_sniffhub
 ├── 📂 lib                  # SQLite JDBC 드라이버 (.jar)
 ├── 📄 sniffhub.db          # 데이터베이스 파일
 ├── 📄 build.gradle         # Gradle 설정 파일
 └── 📂 src
      └── 📂 main
           └── 📂 java
                └── 📂 com.sniffhub
                     ├── 📂 model       # 데이터(DTO), 로직(Service), DB접근(DAO)
                     │    ├── DogManagementModel.java  ★ 핵심 로직
                     │    ├── Dog.java / Owner.java
                     │    └── ...
                     ├── 📂 view        # 화면 UI (Swing)
                     │    ├── Main.java                ★ 실행 파일
                     │    ├── AttendanceKlassListDialog.java
                     │    └── ...
                     ├── 📂 controller  # View와 Model의 중개자
                     │    ├── AttendanceController.java
                     │    └── ...
                     └── 📂 sqlite      # DB 연결 유틸리티 (DBUtil)
